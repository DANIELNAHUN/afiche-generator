version: '3.9'

services:
  front-iglesia-ilo:
    build:
      context: .
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8100}
    container_name: front-iglesia-ilo
    ports:
      - "8101:80"
    restart: unless-stopped
